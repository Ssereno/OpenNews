# Task List for Theme Toggle

- [x] Implement `ThemeService` (Generate service, add logic).
- [x] Define CSS Variables in `styles.scss` for Light/Dark modes.
- [x] Add `mat-icon` registry or ensure icons are available (check `index.html` for Google Fonts/Icons link).
- [x] Update `NewsFeedComponent` template to include the toggle UI.
- [x] Implement toggle method in `NewsFeedComponent`.
- [x] Verify persistence and visual changes.
